# Pin PySpark to match your cluster Spark version when deploying.
pyspark>=3.4.0,<3.6.0
numpy>=1.24.0
pytest>=7.4.0
# PySpark ML imports distutils; Python 3.12+ needs setuptools to supply it.
setuptools>=69.0.0
